# :basketball: Notes and conclusions of `06-concatCSV.ipynb`

## Input
The CSV's used in this notebook as input can be found in the `results`-folder (base, augmented and combined metrics).

## Output
The concatenated CSV's are exported to a new CSV file called `concat_metrics.csv` inside the `results`-folder.
